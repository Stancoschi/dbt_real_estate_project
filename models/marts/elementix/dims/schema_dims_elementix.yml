version: 2

models:
  - name: dim_date
    description: "Standard date dimension table."
    columns:
      - name: date_day
        tests:
          - unique
          - not_null
      - name: year
        tests:
          - not_null